[project]
name = "resonance-osint"
version = "0.1.0"
description = "Evidence-first recursive repository analysis and resonance graph prototype"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
resonance-osint = "resonance_osint.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
